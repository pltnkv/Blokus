/**
 * Created with JetBrains WebStorm.
 * User: oleg
 * Date: 06.09.13
 * Time: 23:15
 * To change this template use File | Settings | File Templates.
 *//*
var log = require('logger')(module);
var log = require('logger')(module);


var GameObject = (function () {
    // "private" variables
    var _title;

    // constructor
    function GameObject(title) {
        _title = title;
    };

    GameObject.prototype.getTitle = function () {
        return _title;
    };

    return GameObject;
})();


var GamesManager = (function () {
    // "private" variables
    var _bar;

    // constructor
    function GamesManager() {
    };

    // add the methods to the prototype so that all of the
    // GamesManager instances can access the private static
    /**
     * @param title
     * @return GameObject
     */
    /*GamesManager.prototype.createGame = function (title) {
        return new GameObject(title);
    };

    return GamesManager;
})();

var gamesManager = new GamesManager();



exports.gamesManager = gamesManager;
     */

